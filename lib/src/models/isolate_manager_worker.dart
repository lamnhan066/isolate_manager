/// A default annotation for the [_IsolateManagerWorker] with empty name.
const isolateManagerWorker = _IsolateManagerWorker();

/// The annotation to generate a Worker JS for a specific method.
class _IsolateManagerWorker {
  /// The annotation to generate a Worker JS for a specific method.
  const _IsolateManagerWorker();
}
