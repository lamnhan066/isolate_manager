/// This is a mixin that provides initial parameters for an isolate manager.
mixin InitialParamsMixin {
  /// Get initial parameters when you create the IsolateManager.
  dynamic get initialParams;
}
